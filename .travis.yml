dist: trusty
sudo: false
language: python
python:
- '3.3'

jdk:
- oraclejdk8

addons:
  sonarqube:
    organization: "timetable"
    token:
      secure: "$SONAR_TOKEN"
services:
- postgres

branches:
  only:
  - master
  - Desenvolvimento

script:
- sonar-scanner
